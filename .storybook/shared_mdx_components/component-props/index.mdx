export const ComponentProps = ({ items }) => (
    <div style={{ overflowX: 'auto' }}>
        <table style={{ width: '100%' }}>
            <thead>
            <tr>
                <th align="left">Props</th>
                <th align="left">
                    Type <small style={{ color: '#999' }}>(* = required)</small>
                </th>
                <th align="left">Default</th>
                <th align="left" style={{ minWidth: '300px' }}>
                    Description
                </th>
            </tr>
            </thead>
            <tbody>
            {items.map(({ propName, type, defaultValue, description }, index) => (
                <tr key={index}>
                    <td align="left">
                        <code>{propName}</code>
                    </td>
                    <td align="left">
                        <code>{type}</code>
                    </td>
                    <td align="left">
                        <code>{defaultValue}</code>
                    </td>
                    <td align="left">{description}</td>
                </tr>
            ))}
            </tbody>
        </table>
    </div>
);
